# Minecraft OCR
要件および設計まとめ。

---

# 目的

Minecraftの画面上に表示されたピクセルフォントを、ゲーム環境を変更せずに外部ツールから読み取る。

- Windows向け
- Pythonで動作
- OCR結果を手動修正してから、任意のタイミングでコピー
- Minecraft側のMODやフォント変更は不要
- 環境を変えても使い回せる構成

---

# 優先度A：初期版に必須

## 1. Minecraft専用OCR

汎用OCRではなく、Minecraftのピクセルフォントに特化した認識方式を使う。

- フォントの字形テンプレートと画面上の文字を比較
- 文字幅・文字送りもフォントプロファイルに保持
- 一文字ずつ単純に切らず、行全体で候補を探索
- 複数行を上から下へ認識
- 英数字と一般的な記号を初期対象とする
- 低一致でも最有力候補を出力
- 文字自体を検出できなかった場合のみ、結果欄を変更しない

### 空白

- 文中の半角スペースを認識
- 連続スペースは半角スペース1個にまとめる
- 行頭・行末の空白は削除
- 改行は維持

```text
Name:    Steve
↓
Name: Steve
```

### 対象フォント
- MinecraftのJava版のフォントに限定する
- ASCII（英数字・基本記号）のみを対象とする
- 英数字と基礎的な記号を含む94文字が対象

### フォント画像ファイルの読み込み
元のascii.pngを毎回解析せず、一度だけOCR用データに変換・保存し、通常実行では変換済みデータを読む。
設定ファイル内に文字データのファイル名を外だしする。
```json
{
  "source_profile": "profiles/java_default_ascii.json",
  "font_data": "data/compiled/java_default_ascii.npz",
}
```

### 認識モード
軽量化のため精度を調整するため認識モードを選択可能にする。<br>
二値化処理は共通。文字列検索のプロファイルを2つ作り、ユーザーがドロップダウンで選べるようにする。
各プロファイルの数値は、文字認識部分のプロトタイプで実際に試した結果をもとにしている。

- 高速（起動時標準）
```json
{
  "max_auto_colors": 2,
  "vertical_search": 0,
  "beam_width": 1,
  "max_cost_per_glyph": 0
}
```
- 正確
```json
{
  "max_auto_colors": 4,
  "vertical_search": 8,
  "beam_width": 10,
  "max_cost_per_glyph": 1.0
}
```

- 選択値を認識処理に使用
- 設定を終了後も保持
- 自動判定処理の実装予定はない

ただし、切り抜き開始位置が文字のピクセル格子からずれることはあるため、認識器側でGUIサイズ内のX・Yオフセットを試す。

## 2. GUIサイズの手動指定

MinecraftのGUIサイズは自動判定しない。

```text
GUIサイズ
[ 2 ▼ ]

・1
・2
・3
・4
```

## 3. 対象モニターの手動指定

マルチモニターは、読み取る対象をドロップダウンで選択する。

```text
対象モニター
[ 1: 2560×1440・メイン ▼ ]
```

- 指定モニター内だけ範囲選択可能
- モニターをまたぐ選択には対応しない
- モニター名・解像度・位置情報を保存
- 保存したモニターが存在しなければメインモニターへフォールバック

## 4. アプリ内の範囲選択

Windowsの範囲選択スクリーンショットに近い操作感にする。

### 起動方法

- アプリ内の「範囲を選択して読み取る」ボタン
- ユーザーが設定できるグローバルホットキー

常時監視はしない。起動中のみホットキーを有効にする。

### 選択時の流れ

1. 選択対象モニターを撮影
2. メインウィンドウを隠す
3. 撮影した静止画を対象モニター全体に表示
4. 画面全体を薄暗くする
5. マウスドラッグで範囲選択
6. 選択範囲だけ明るく表示
7. マウスを離したらOCR
8. `Esc`または右クリックでキャンセル

重要なのは、**ツールチップなどが消える前に撮影し、その静止画上で選択すること**。

- 選択画像はメモリ上で処理
- クリップボードを経由しない
- ファイルへ自動保存しない
- 範囲選択中のホットキー連打を防止

## 5. 編集可能な結果入力欄

OCR結果は表示専用ではなく、編集可能な複数行テキスト入力欄へ反映する。

- OCR結果を直接修正可能
- 通常の入力・削除・選択が可能
- `Ctrl+A`、`Ctrl+C`、`Ctrl+V`
- Undo／Redo対応
- 改行を保持
- 結果入力欄がウィンドウの余った領域を使用

## 6. OCR結果の反映方法

ドロップダウンで次の3択にする。

```text
結果の反映方法
[ 改行して末尾に追加 ▼ ]

・置き換える
・末尾に追加
・改行して末尾に追加
```

起動時のデフォルトは、**改行して末尾に追加**。

### 置き換える

```text
現在：Hello
認識：World
結果：World
```

### 末尾に追加

```text
現在：Hello
認識：World
結果：HelloWorld
```

### 改行して末尾に追加

```text
現在：Hello
認識：World

結果：
Hello
World
```

- 入力欄が空なら先頭に改行を入れない
- すでに末尾が改行なら、余計な改行を重ねない

## 7. 手動コピーのみ

クリップボードは、ユーザーが明示的に操作したときだけ変更する。

- 自動コピー機能は設けない
- 自動コピー設定も設けない
- OCR完了時にはクリップボードを変更しない
- 「コピー」ボタンは、その時点の入力欄全文をコピー
- 通常の`Ctrl+C`は選択部分のみコピー
- 入力欄が空ならコピーボタンを無効化可能

Windowsのクリップボード履歴を不要なOCR結果で散らかさないことを優先する。

## 8. 読み取り一致度の表示

文字ごとの色付けは行わず、今回の読み取り全体の一致度だけを表示する。

```text
今回の一致度：87 / 100　● 高一致
```

暫定的な色分け：

| 一致度 | 色 |
|---:|---|
| 90～100 | 緑 |
| 75～89 | 黄緑 |
| 55～74 | オレンジ |
| 0～54 | 赤 |

- 厳密な正解確率ではなく、画像と字形の「一致度」
- 閾値は実際の画像で調整
- 低一致でも最有力結果を入力欄へ反映
- 文字が検出されなかった場合は`—`
- 検出されなかった場合、入力欄は変更しない

```text
今回の一致度：—
文字を検出できませんでした
```

## 9. 最新の追加範囲を強調

文字ごとの怪しい箇所ではなく、**今回追加された範囲全体**を薄い青などで強調する。

### 置き換える

- 強調なし
- 全文が今回の結果なので区別不要
- 以前の強調を解除

### 末尾に追加

- 今回追加した文字列だけ強調

### 改行して末尾に追加

- 今回追加したOCR結果だけ強調
- 区切りとして自動挿入した改行は強調しない

共通動作：

- 次の認識成功時に、強調を新しい範囲へ移動
- 認識失敗時は既存の強調を変更しない
- 強調色はコピー内容に含まれない

## 10. 自動スクロール設定

```text
☑ 認識結果を反映した位置へ自動スクロール
```

- ON／OFF可能
- 初期状態はON
- 設定を終了後も保持

### ON

OCR結果を追加した位置が見えるようにスクロールする。

### OFF

- 現在の表示位置を可能な限り維持
- カーソルや選択範囲を勝手に移動しない

この設定はOCR結果を反映したときだけ適用する。

---

# 優先度B：安定性のため初期版にできるだけ入れる

## 11. 可変ウィンドウサイズ

- ウィンドウを縦横にリサイズ可能
- 最大化可能
- 最小サイズを設定
- 設定部分やボタンは必要以上に拡大しない
- 増えた領域は結果入力欄へ割り当てる

保存対象：

- ウィンドウ位置
- ウィンドウサイズ
- 最大化状態

以前のモニターがなくなった場合は、現在の画面内へ戻す。

## 12. リサイズ時の表示位置維持

リサイズ前に表示されていた**先頭の表示行**を維持する。

- 上端で半分だけ見えている行も、維持対象の先頭行とする
- リサイズ後は、その行を完全に表示
- 横幅変更で折り返し位置が変わる場合は、文字位置をアンカーにする
- カーソル位置と選択範囲を維持
- 文末付近では、下に不自然な表示上の余白ができても構わない
- 下余白はテキストやコピー内容に含めない
- 自動スクロール設定とは独立して動作

## 13. 認識前処理

最低限、次を考慮する。

- 選択範囲を少し自動拡張し、文字端の切断を防止
- GUIサイズに応じたピクセル格子のずれ補正
- 文字色と背景の分離
- 影あり・影なし両方を試す
- 固定RGBではなく、周囲との色差も使用
- 暗い色や半透明文字にもある程度対応
- 複数行の行分離
- ノイズや小さな点を句読点と誤認しすぎない処理

## 14. OCR失敗時の安全動作

次の場合、入力欄を変更しない。

- 範囲選択をキャンセル
- 選択範囲が小さすぎる
- スクリーンショット取得失敗
- 文字画素そのものが見つからない

一方、文字候補が作れた場合は、一致度が低くても最有力結果を反映する。

## 15. 設定の保存

少なくとも以下を保持する。

```json
{
  "monitor": "...",
  "gui_scale": 3,
  "hotkey": "<ctrl>+<shift>+r",
  "auto_scroll": true,
  "window_geometry": "...",
  "window_maximized": false
}
```

結果反映方法は、起動時に常に「改行して末尾に追加」とする。

---

# 優先度C：初期版の後に追加

## 17. 太字・斜体など

後から対応する候補：

- 太字
- 斜体
- 下線
- 影の異なる表示

難読化されたランダム文字の復元、特殊文字や絵文字は対象外。

## 19. 選択画像の手動保存

自動保存はしない。

必要になった場合だけ、

```text
[今回の選択画像を保存]
```

を用意し、認識不具合の検証に使う。

## 20. 辞書・補正機能

辞書は有効だが、初期版では入れない。

将来追加する場合：

- 頻出単語・フレーズ
- Minecraft標準用語
- MODごとの辞書
- 誤認識から正しい語への補正
- 辞書のインポート／エクスポート
- 結果欄の選択範囲から手動登録

辞書は強制置換ではなく、字形候補が近い場合の補助として使う。

---

# 初期版の対象範囲

## 対応対象

- MinecraftのGUI上の文字
- チャット
- メニュー
- ツールチップ
- 字幕
- 正面向きの本など
- 横書き
- 英数字・一般的な記号
- 複数行

## 初期版では対象外

- ワールド内の看板
- 遠近や傾きのある文字
- 頭上のネームタグ
- 複数列の自動読順判定
- 難読化文字の復元
- GUIサイズの自動判定
- モニターをまたぐ範囲選択
- クリップボード監視
- 自動コピー
- 自動翻訳
- 辞書補正

---

# 実装順

**追記：字形認識プロトタイプが成功したので、実装順を変更する。現在正常に動作しているプロトタイプがあるので、字形認識など根幹機能は、すでに実装されているものを流用する。差分のみ新規実装・変更する。**

1. **メインウィンドウと設定保存**
2. **対象モニター指定**
3. **ボタン／ホットキーから範囲選択**
4. **選択画像を取得してプレビュー**
5. **編集可能な結果欄と3種類の反映方法**
6. **コピー・強調・一致度表示**
7. **Minecraft標準フォントの字形認識**
8. **スペース・複数行・影への対応**
9. **実際のスクリーンショットで一致度を調整**
10. **必要に応じてフォントプロファイルなどを拡張**

初期目標としては、 **「指定モニターから範囲を選び、Java標準フォントの英文を読み、編集可能な結果欄へ追加する」** ところまで作れば、実用性を評価できる。